Your cluster "{{ .Kind.Name }}" is ready.

Jaeger UI is available at http://localhost:30686 (in-memory storage).

  open http://localhost:30686

OTLP endpoints:
  gRPC  localhost:30317
  HTTP  http://localhost:30318
