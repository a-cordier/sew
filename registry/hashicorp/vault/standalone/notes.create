Your cluster "{{ .Kind.Name }}" is ready.

Vault is running in dev mode at http://localhost:30820 (root token: root).

  curl http://localhost:30820/v1/sys/health
  export VAULT_ADDR=http://localhost:30820
  export VAULT_TOKEN=root
  vault status
