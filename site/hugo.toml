baseURL = "https://a-cordier.github.io/sew/"
languageCode = "en-us"
title = "sew"

[params]
  description = "Your local Kubernetes tailor shop"
  tagline = "sew provisions local Kubernetes clusters from a registry of composable application stacks. Each stack packages its Helm charts, manifests, networking, and dependencies into a single deployable unit. One command sets everything up."
  githubRepo = "https://github.com/a-cordier/sew"
  pinnedGroups = ["gravitee-io"]

[menu]
  [[menu.main]]
    name = "Docs"
    url = "/docs/"
    weight = 1
  [[menu.main]]
    name = "Registry"
    url = "/registry/"
    weight = 2
  [[menu.main]]
    name = "GitHub"
    url = "https://github.com/a-cordier/sew"
    weight = 3

[markup]
  [markup.highlight]
    style = "dracula"
    lineNos = false
  [markup.tableOfContents]
    startLevel = 2
    endLevel = 2
  [markup.goldmark]
    [markup.goldmark.renderer]
      unsafe = true

[outputs]
  home = ["HTML"]
  section = ["HTML"]
